version: "3.9"

services:
  smtp-server:
    image: ${IMAGE_NAME}:latest
    container_name: inbound-mail
    env_file:
      - .env
    ports:
      - "25:25" # Inbound SMTP
      - "587:587" # Optional submission/STARTTLS
    restart: unless-stopped
